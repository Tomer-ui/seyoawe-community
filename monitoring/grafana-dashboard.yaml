# Grafana community dashboard 18283 — "Kubernetes cluster monitoring (via Prometheus)"
# Downloaded at boot time by the Grafana deployment's init container.
# This ConfigMap is no longer used for dashboard data; the init container
# fetches the latest revision directly from grafana.com and writes it to
# an emptyDir volume shared with the Grafana container.
#
# Kept as a reference placeholder. See grafana-deployment.yaml for the
# init container that performs the download.
#
# Dashboard: https://grafana.com/grafana/dashboards/18283
