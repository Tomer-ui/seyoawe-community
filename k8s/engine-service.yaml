apiVersion: v1
kind: Service
metadata:
  name: seyoawe-engine
  namespace: default
spec:
  selector:
    app: seyoawe-engine
  ports:
    - port: 8080
      targetPort: 8080
  type: NodePort    # exposes the engine within the cluster; Ingress sits in front for external traffic
