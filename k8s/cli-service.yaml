apiVersion: v1
kind: Service
metadata:
  name: seyoawe-cli
  namespace: default
spec:
  selector:
    app: seyoawe-cli
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP    # CLI is an internal utility — not exposed externally
